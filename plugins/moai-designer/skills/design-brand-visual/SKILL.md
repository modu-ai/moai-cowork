---
name: design-brand-visual
description: |
  브랜드 자료에 맞는 히어로·OG·목업·마스코트 이미지의 구도와 제약을 정하고 실제 이미지 생성 경로로 보냅니다.
  ChatGPT Work의 기본 이미지 생성은 moai-media:media-codex-image, Higgsfield 지정 요청은 moai-media:media-higgsfield-image를 사용합니다.
  "브랜드 히어로 이미지 만들어줘", "OG 이미지 생성해줘", "마스코트 다른 포즈로 그려줘" 같은 요청에 사용하세요.
version: "1.1.2"
---

# 브랜드 비주얼

사용자의 이미지 용도와 브랜드 규칙을 실제 생성 도구에 전달할 수 있는 구체적인 제약으로 정리한다. 사용자가 이미 생성을 요청했고 세션에 도구가 있으면 프롬프트만 반환하고 끝내지 않는다.

## 브랜드 자료 확인

1. 접근 가능한 디자인 시스템 핸드오프, `DESIGN.md`, 토큰 파일, 브랜드 가이드, 마스코트 원본을 확인한다. 파일이 없으면 사용자가 제공한 색·서체·참조 이미지를 사용한다.
2. 팔레트, 형태, 여백, 분위기, 이미지 안 문구, 마스코트의 허용된 변형을 정리한다. 근거 없는 색·로고·제품 UI를 브랜드의 확정값처럼 만들지 않는다.
3. 실제 사용 위치의 크기·비율·카피 공간을 확인한다. 히어로·OG·카드의 화면비를 일률적으로 고정하지 않는다.
4. 기존 마스코트나 제품 이미지를 편집한다면 원본이 도구에 실제 첨부됐는지 확인하고 변경할 부분과 보존할 부분을 분리한다.

## 생성 경로

| 사용자 요청과 현재 호스트 | 실행 경로 |
|---|---|
| ChatGPT Work의 기본 이미지 생성·편집 | `moai-media:media-codex-image`와 현재 대화의 이미지 생성 도구. [ChatGPT Images 2.5 공식 안내](https://help.openai.com/en/articles/11084440-images-in-chatgpt)를 따른다. |
| Higgsfield 계정·모델·크레딧을 명시 | `moai-media:media-higgsfield-image`와 공식 Higgsfield 연결. 모델·비용·파라미터는 라이브 조회와 해당 스킬의 절차를 따른다. |
| Claude Cowork에서 Higgsfield 연결 사용 | `moai-media:media-higgsfield-image`를 사용한다. 연결이 없으면 실제 생성 가능 여부를 밝히고 프롬프트를 제공한다. |
| 프롬프트만 요청 | 선택한 경로에 맞는 프롬프트와 브랜드 제약을 제공한다. |

일반 ChatGPT 이미지 요청을 Higgsfield로 자동 전환하지 않는다. 반대로 Higgsfield를 지정한 요청을 ChatGPT 기본 이미지 도구로 임의 전환하지 않는다. 앱 도구가 실제 API 모델 ID를 노출하지 않으면 결과에 특정 API 모델 ID를 붙이지 않는다. 여러 컷의 동일 인물·마스코트 참조가 필요하고 Higgsfield를 선택했다면 `moai-media:media-higgsfield-identity`의 실제 지원 범위를 확인한다.

## 결과 검수

생성된 이미지를 실제로 보고 색·형태·구도·로고·문구·인물 일관성을 확인한다. 이미지 속 텍스트는 오탈자를 점검한다. 배경 위에 웹 텍스트를 얹는다면 최종 화면에서 대비를 검사한다. 어긋난 항목은 관찰한 내용으로 기록하고 재생성이나 편집을 제안한다. 생성 도구 호출, 결과 확인, 파일 저장을 각각 별도 완료 상태로 보고한다. 실패가 모호하면 결과 상태를 먼저 확인하고 중복 생성하지 않는다.

출력에는 용도, 참조 자료, 적용 제약, 실제 사용한 생성 경로, 결과 이미지 또는 파일 경로, 검수 결과를 적는다. 결과가 없으면 생성 완료라고 말하지 않는다.
